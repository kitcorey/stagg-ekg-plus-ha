services:
  homeassistant:
    image: ghcr.io/home-assistant/home-assistant:${HA_VERSION:-stable}
    container_name: ha-fellow-stagg-dev
    restart: unless-stopped
    volumes:
      - ./config:/config
      - ./custom_components/fellow_stagg:/config/custom_components/fellow_stagg:ro
    environment:
      - TZ=${TZ:-UTC}
    ports:
      - "${HA_PORT:-8123}:8123"
